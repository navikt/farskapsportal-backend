spring:
  cloud.gcp:
    sql.enabled: false
    gcp.secretmanager.enabled: false
    bootstrap.enabled: true
  config.activate.on-profile: test, local, local-postgres, remote-postgres, scheduled-test, skatt-ssl-test